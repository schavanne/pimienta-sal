<!DOCTYPE html>
<html>
  <!-- Head -->
  <%- include('./partials/head',{
    title: 'Home'
  }) %>

  <body>
    <!-- Navbar (sit on top) -->
    <%- include('./partials/navbar') %> 

    <!-- Header -->
    <%- include('./partials/header') %>

    <!-- Page content -->
    <div class="ps-content">
      <!-- About Section -->
      <div class="ps-row ps-padding-64" id="about">
        <div class="ps-col m6 ps-padding-large ps-hide-small">
          <img
            src="/images/principal.jpg"
            class="ps-round ps-image ps-opacity-min"
            alt="SAbores que enamoran"
            width="600"
            height="750"
          />
        </div>

        <div class="ps-col m6 ps-padding-large">
          <h1 class="ps-center"><%= about.title %> </h1>
          <br />
          <p class="ps-large">
            <%- about.text %>
          </p>
          <p class="ps-large ps-text-grey ps-hide-medium">
            <%= about.subtext %>
          </p>
        </div>
      </div>

      <hr />
      <!-- Menu Section -->
      <div class="ps-row ps-padding-64" id="menu">
        <div class="ps-col l6 ps-padding-large">
          <h1 class="ps-center">Nuentro Menu</h1>
          <br />
          <% products.forEach(product => { %>
            <h4><a href="/detail/<%= product.id %>"><%= product.title %></a></h4>
            <p class="ps-text-grey">
              <%= product.description %> U$S <%= product.price %> 
            </p>
            <br />
          <% }) %>
        </div>

        <div class="ps-col l6 ps-padding-large ps-img-menu">
          <img
            src="/images/menu.jpg"
            class="ps-round ps-image ps-opacity-min"
            alt="Menu"
          />
        </div>
      </div>

      <!-- Footer -->
      <footer class="ps-center ps-light-grey ps-padding-32">
        <p>Powered by Pimienta & Sal</p>
      </footer>
    </div>
  </body>
</html>
